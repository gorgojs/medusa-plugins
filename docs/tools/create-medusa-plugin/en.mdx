---
title: "create-medusa-plugin"
description: "A CLI tool to quickly scaffold a Medusa plugin and example projects in a ready-to-develop monorepo structure"
---

# {frontmatter.title}

A CLI tool to quickly scaffold a Medusa plugin and example projects in a ready-to-develop monorepo structure.

## Usage

`create-medusa-plugin` uses the official [`create-medusa-app`](https://docs.medusajs.com/resources/create-medusa-app) under the hood to scaffold Medusa projects—including the plugin, backend and storefront folders. This ensures your setup follows official best practices and remains fully compatible with the Medusa ecosystem by leveraging the official Medusa app generator.

```bash title="Terminal"
npx create-medusa-plugin [plugin-name] [options]
```

Replace `[plugin-name]` with your desired plugin name.

### Options

<MedusaTypeList
  sectionTitle="Options"
  types={[
    {
      name: "-r, --repo <name>",
      type: "string",
      description: "Root repository (monorepo) name.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "-e, --example <name>",
      type: "string",
      description: "Example folder name, where the `medusa` and `medusa-storefront` examples will be created.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "--directory-path <path>",
      type: "string",
      description: "Path to the output directory for the root repository.",
      optional: true,
      defaultValue: "Current directory",
      expandable: false,
      children: []
    },
    {
      name: "--plugin-repo-url <url>",
      type: "string",
      description: "URL of repository to setup a Medusa plugin (forwards `repo-url` to `create-medusa-app`).",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "--example-repo-url <url>",
      type: "string",
      description: "URL of repository to setup an example Medusa backend (forwards `repo-url` to `create-medusa-app`).",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "--no-storefront",
      type: "boolean",
      description: "Do not include example storefront (forwards as `--with-nextjs-starter=false` to `create-medusa-app`).",
      optional: true,
      defaultValue: "false",
      expandable: false,
      children: []
    },
    {
      name: "--skip-example-db",
      type: "boolean",
      description: "Skip creating the Medusa example database (forwards `--skip-db` to `create-medusa-app`).",
      optional: true,
      defaultValue: "false",
      expandable: false,
      children: []
    },
    {
      name: "--verbose",
      type: "boolean",
      description: "Show all logs of underlying commands (forwards `--verbose` to `create-medusa-app`).",
      optional: true,
      defaultValue: "false",
      expandable: false,
      children: []
    },
  ]}
/>

---

## Examples

### Basic usage

```bash title="Terminal"
npx create-medusa-plugin
```

This command will prompt you for the plugin name and other configuration options interactively.

### With options

```bash title="Terminal"
npx create-medusa-plugin my-plugin -r my-plugin-monorepo -e my-plugin-example --skip-example-db
```

This command scaffolds the following monorepo structure, installing the Medusa projects within it:

```txt title="Monorepo structure"
|
└── my-plugin-monorepo/
    ├── examples/
    │   └── my-plugin-example/
    │      ├── medusa/
    │      └── medusa-storefront/
    ├── packages/
    │   └── my-plugin/
    ├── package.json
    └── README.md
```

### With a plugin scope

```bash title="Terminal"
npx create-medusa-plugin @my-scope/my-plugin -r my-plugin-monorepo -e my-plugin-example --skip-example-db
```

This command scaffolds the following monorepo structure, placing your scoped plugin in a nested folder under `packages` and installing the Medusa projects within it:

```txt title="Monorepo structure"
|
└── my-plugin-monorepo/
    ├── examples/
    │   └── my-plugin-example/
    │      ├── medusa/
    │      └── medusa-storefront/
    ├── packages/
    │   └── @my-scope/
    │      └── my-plugin/
    ├── package.json
    └── README.md
```
