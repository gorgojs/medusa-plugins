---
title: "Getting Started with Robokassa Payment for Medusa"
description: "In this document, you'll learn how to install and configure the Robokassa payment provider for Medusa"
---

# {frontmatter.title}

In this document, you'll learn how to install and configure the Robokassa payment provider for Medusa.

## Requirements

- Medusa v2.7.0 or later
- Node.js v20 or later
- A Robokassa account – [sign in or create one](https://login.robokassa.ru/reg?promoCode=gorgo)

## Installation

```bash
yarn add @gorgo/medusa-payment-robokassa
# or
npm install @gorgo/medusa-payment-robokassa
```

## Configuration

Add the provider configuration in your `medusa-config.js` file of the Medusa admin application:

```ts
// ...
module.exports = defineConfig({
  // ...
  modules: [
    {
      resolve: "@medusajs/medusa/payment",
      options: {
        providers: [
          {
            resolve: "@gorgo/medusa-payment-robokassa/providers/payment-robokassa",
            id: "robokassa",
            options: {
              merchantLogin: process.env.ROBOKASSA_MERCHANT_LOGIN,
              hashAlgorithm: process.env.ROBOKASSA_HASH_ALGORITHM,
              password1: process.env.ROBOKASSA_PASSWORD_1,
              password2: process.env.ROBOKASSA_PASSWORD_2,
              testPassword1: process.env.ROBOKASSA_TEST_PASSWORD_1,
              testPassword2: process.env.ROBOKASSA_TEST_PASSWORD_2,
              capture: false,  // default is true
              isTest: true,  // default is false
            },
          }
        ]
      }
    }
  ]
})
```

Add environment variables with your shop identifier `merchantLogin`, hash calculation algorithm `hashAlgorithm`, secret passwords `password1`, `password2`, and secret passwords for testing `testPassword1`, `testPassword2`:

```text
ROBOKASSA_MERCHANT_LOGIN=test-shop
ROBOKASSA_HASH_ALGORITHM=md5
ROBOKASSA_PASSWORD_1=supersecret
ROBOKASSA_PASSWORD_2=supersecret
ROBOKASSA_TEST_PASSWORD_1=supersecret
ROBOKASSA_TEST_PASSWORD_2=supersecret
```

> **Attention!** `ROBOKASSA_HASH_ALGORITHM` must be one of the following values corresponding to the value in the Robokassa account: `md5`, `sha1`, `sha256`, `sha384`, `sha512` or `ripemd160` (note, an error occurs for `ripemd160` on the provider's side)

## Provider Options

<MedusaTypeList
  sectionTitle="ProviderOptions"
  types={[
    {
      name: "options",
      type: "object",
      description: "Provider configuration options",
      optional: false,
      expandable: false,
      children: [
        {
          name: "merchantLogin",
          type: "string",
          description: "The Robokassa store ID that you created when creating the store.",
          optional: false,
          expandable: false,
          children: []
        },
        {
          name: "hashAlgorithm",
          type: "enum",
          description: "Signature algorithm used to compute and verify payment signatures:\n- `md5`\n- `sha1`\n- `sha256`\n- `sha384`\n- `sha512`",
          optional: false,
          expandable: false,
          children: []
        },
        {
          name: "password1",
          type: "string",
          description: "Technical password #1 used to compute the signature when initiating a payment.",
          optional: false,
          expandable: false,
          children: []
        },
        {
          name: "password2",
          type: "string",
          description: "Technical password #2 used to verify the signature in the payment notification.",
          optional: false,
          expandable: false,
          children: []
        },
        {
          name: "testPassword1",
          type: "string",
          description: "Test password #1 (from store settings) used to compute signatures in test mode.\nApplicable only if `isTest` = `true`",
          optional: true,
          expandable: false,
          children: []
        },
        {
          name: "testPassword2",
          type: "string",
          description: "Test password #2 used to verify notifications in test mode.\nApplicable only if `isTest` = `true`",
          optional: true,
          expandable: false,
          children: []
        },
        {
          name: "capture",
          type: "boolean",
          description: "Automatic payment capture:\n- `true` for one-step payment\n- `false` for two-step payment (pre-authorization)",
          optional: true,
          defaultValue: "true",
          expandable: false,
          children: []
        },
        {
          name: "isTest",
          type: "boolean",
          description: "Enables test mode.",
          optional: true,
          defaultValue: "false",
          expandable: false,
          children: []
        },
        {
          name: "useReceipt",
          type: "boolean",
          description: "Enable receipt generation according to Russian fiscal data format (FFD).",
          optional: true,
          defaultValue: "false",
          expandable: false,
          children: []
        },
        {
          name: "taxation",
          type: "enum",
          description: "Tax system type:\n- `osn`: General\n- `usn_income`: Simplified (income)\n- `usn_income_outcome`: Simplified (income-expenses)\n- `esn`: Agricultural\n- `patent`: Patent\nApplicable only if `useReceipt` = `true`",
          optional: true,
          expandable: false,
          children: []
        },
        {
          name: "taxItemDefault",
          type: "enum",
          description: "Default VAT rate for products:\n- `none`: No VAT\n- `vat0`: 0%\n- `vat5`: 5%\n- `vat7`: 7%\n- `vat10`: 10%\n- `vat20`: 20%\n- `vat105`: 5/105\n- `vat107`: 7/107\n- `vat110`: 10/110\n- `vat120`: 20/120\nApplicable only if `useReceipt` = `true`",
          optional: true,
          expandable: false,
          children: []
        },
        {
          name: "taxShippingDefault",
          type: "enum",
          description: "Default VAT rate for shipping, same options as `taxItemDefault`.\nApplicable only if `useReceipt` = `true`",
          optional: true,
          expandable: false,
          children: []
        }
  
      ]
    }
  ]}
/>

## Webhook Setup

To properly handle payment notifications from Robokassa, configure the webhook URL in your Robokassa account as following:

Under shop settings in your Robokassa account, set the **Method of sending data to Result Url** to `GET` or `POST` and supply a **Result Url** in the following format:

```
https://{YOUR_MEDUSA_DOMAIN}/hooks/payment/robokassa_robokassa
```

Robokassa will send payment status updates to this URL, allowing Medusa to update the payment status accordingly.
